# ─── Honey Chain – Dependencies (Phase 1–3) ───────────────────────────────
# Python 3.11+

# Web framework
fastapi==0.115.12

# ASGI server
uvicorn[standard]==0.34.3

# Settings / environment variable management
pydantic-settings==2.9.1

# Email validation (required by Pydantic EmailStr)
email-validator==2.2.0

# Database ORM + driver
sqlalchemy==2.0.41
psycopg2-binary==2.9.10

# Database migrations
alembic==1.16.2

# Password hashing
passlib[bcrypt]==1.7.4
bcrypt==4.0.1             # passlib 1.7.4 is incompatible with bcrypt 5.x

# JWT
python-jose[cryptography]==3.3.0

# QR code generation (Phase 5)
qrcode[pil]==8.2
pillow==12.3.0

# Testing
pytest==8.3.5
httpx==0.28.1      # required by FastAPI TestClient (async-capable)

